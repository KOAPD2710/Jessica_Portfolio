---
import { getPage } from '@/js/core/prismic'
import FooterComponent from '.'
const { data } = await getPage("global_information");
---

<FooterComponent
    {...data}
    client:visible={{ rootMargin: "100% 0px 100% 0px" }}
/>